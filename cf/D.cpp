#include <bits/stdc++.h>
using namespace std;

int T, n = 0;

int main()
{
    cin >> T;
    while (T--)
    {
        cin >> n;
        vector<vector<int>> result;
        vector<int> path;

        vector<vector<int>> rc;
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < i; j++)
            {
                rc[]
            }
        }
    }
}